import React from 'react';
import { View } from 'react-native';
import { useStyles } from './Avatar.useStyles';

const Avatar = ({  }: IAvatarProps) => {
  const { styles, theme } = useStyles();

  return (
    <View>
      
    </View>
  );
};

export interface IAvatarProps {
  
};

export default Avatar;
