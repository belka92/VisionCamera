import React from 'react';
import { View } from 'react-native';
import { useStyles } from './NavigateComponent.useStyles';

const NavigateComponent = ({  }: INavigateComponentProps) => {
  const { styles, theme } = useStyles();

  return (
    <View>
      
    </View>
  );
};

export interface INavigateComponentProps {
  
};

export default NavigateComponent;
